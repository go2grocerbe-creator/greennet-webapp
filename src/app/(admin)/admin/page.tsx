export default function AdminDashboardPage() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-semibold">Admin dashboard</h1>
      <p className="text-muted-foreground mt-2">
        Placeholder only. Content management (services/products/projects), leads, media, site
        settings, and audit log views are not built yet — see docs/architecture.md.
      </p>
    </div>
  );
}
